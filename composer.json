{
  "require": {
    "php": "^8.3",
    "psr/http-message": "^2.0",
    "nyholm/psr7": "^1.8",
    "monolog/monolog": "^3.0",
    "symfony/process": "^7.0"
  },
  "suggest": {
    "ext-swoole": "Required for async HTTP server (install via: pecl install swoole)"
  },
  "autoload": {
    "psr-4": {
      "App\\Utils\\": "class/"
    },
    "files": [
      "./strict_helpers/functions.php"
    ]
  },
  "require-dev": {
    "phpstan/phpstan": "^2.0"
  }
}
